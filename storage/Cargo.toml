[package]
name = "storage"
version = "0.1.0"
edition = "2021"

[dependencies]
hex.workspace = true
sled.workspace = true
directories.workspace = true
errors = { path = "../errors" }
config = { path = "../config" }
